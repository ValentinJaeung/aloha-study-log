# ACT로 양팔 로봇의 물체 전달을 학습시키기

**시뮬레이션 모방학습 실험 보고서 — `sim_transfer_cube_scripted`**

| | |
|---|---|
| 작성일 | 2026-09-18 |
| 대상 독자 | ACT·모방학습·ALOHA를 처음 접하는 사람 |
| 실험 기간 | 2026-09-16 ~ 2026-09-18 (환경 구축 → 데이터 생성 → 학습 → 평가) |
| 사용 코드 | [tonyzhaozh/act](https://github.com/tonyzhaozh/act) (원본, 경로 설정 2개 파일만 수정) |
| 결과 한 줄 | 코드가 만든 데모 50개로 학습한 정책이 **50회 중 47~49회 성공 (94% / 98%)** |

---

## 목차

1. [기 — 무엇을, 왜 하려고 했는가](#1-기--무엇을-왜-하려고-했는가)
2. [승 — 실험을 어떻게 설계했는가](#2-승--실험을-어떻게-설계했는가)
3. [전 — 무엇이 나왔는가](#3-전--무엇이-나왔는가)
4. [결 — 무엇을 알게 되었는가](#4-결--무엇을-알게-되었는가)
5. [확인하지 못한 것](#5-확인하지-못한-것--해석은-읽는-사람의-몫)
6. [부록](#부록-a--재현-명령)

---

# 1. 기 — 무엇을, 왜 하려고 했는가

## 1.1 배경: 값싼 로봇으로 정밀한 일을 시킬 수 있는가

로봇 팔로 물건을 집어 반대쪽 손에 건네는 일은 사람에게는 쉽지만 로봇에게는 어렵습니다.
여유 간격이 1cm 남짓이라 조금만 어긋나도 충돌하거나 놓칩니다. 전통적으로 이런 정밀 작업은
비싼 고정밀 하드웨어로 해결해 왔습니다.

ALOHA 논문(Zhao et al., 2023)은 반대 방향을 택했습니다. **하드웨어는 싸게 두고, 부정확함을
학습으로 메우자**는 것입니다. 약 2만 달러짜리 양팔 시스템을 만들고, 사람이 직접 조종해
만든 데모를 그대로 흉내 내도록 신경망을 학습시켰습니다. 이때 쓰인 알고리즘이 **ACT(Action
Chunking with Transformers)**이고, 논문은 10분 분량의 데모만으로 6개 과제에서 80~90%
성공률을 보고했습니다.

이 보고서는 그 ACT를, 실물 로봇 없이 **시뮬레이션에서 처음부터 끝까지 직접 돌려본 기록**입니다.

## 1.2 ACT는 어떤 알고리즘인가

ACT를 이해하는 데 필요한 개념은 세 가지입니다.

### (1) Action chunking — 한 스텝이 아니라 100스텝을 한꺼번에 예측한다

보통의 모방학습은 "지금 화면을 보고 → 다음 한 스텝의 행동"을 예측합니다. 이 방식의 문제는
**오차 누적(compounding error)**입니다. 50Hz로 제어하면 8초짜리 작업에 400번 예측해야 하고,
한 번 삐끗하면 학습 데이터에 없던 상태로 빠져 그다음부터 계속 어긋납니다.

ACT는 매번 **앞으로의 100스텝(= 2초)을 통째로 예측**합니다. 예측 횟수가 400번에서 4번으로
줄어드니 오차가 누적될 기회 자체가 100분의 1로 줄어듭니다. 논문의 ablation에서 이 chunk
크기 `k`를 1에서 100으로 키우자 성공률이 **1% → 44%**로 올랐습니다(2개 시뮬 태스크 ×
scripted/human 데이터, 총 4개 설정의 평균값).

### (2) Temporal ensembling — 이어 붙인 자국을 지운다

100스텝짜리 덩어리를 이어 붙이면 이음매에서 동작이 튑니다. 그래서 ACT는 선택적으로
**매 스텝마다 새로 예측하고, 같은 시점을 가리키는 과거 예측들을 가중평균**합니다.

가중치는 `w_i = exp(-m·i)` (오래된 예측일수록 작은 가중치, 코드상 `m = 0.01`,
[`imitate_episodes.py:252-256`](../act/imitate_episodes.py#L252-L256)).

**이건 학습이 아니라 실행 시점의 옵션입니다.** 같은 체크포인트를 켜고 끄고 쓸 수 있습니다.
이 실험의 핵심 비교 축이 바로 이것입니다.

### (3) CVAE — 사람 데모가 제각각인 문제를 다루기 위한 장치

사람이 같은 작업을 10번 시연하면 궤적이 10번 다 다릅니다(멀티모달). 이를 단순 평균 내면
어느 쪽도 아닌 엉뚱한 동작이 나옵니다. ACT는 이를 다루려고 **CVAE**를 씁니다. 학습할 때만
동작하는 인코더가 "이 데모는 어떤 스타일인가"를 32차원 잠재변수 `z`로 압축하고, 실행할 때는
`z = 0`(사전분포의 평균)으로 고정합니다.

> ⚠️ **이 실험에서는 CVAE가 사실상 놀고 있습니다.** 뒤에서 다시 다룹니다([4.3](#43-한계)).

### (4) ACT는 강화학습이 아니다 — 자주 하는 오해

손실 함수는 `L1(정답 행동, 예측 행동) + β × KL` 이 전부이고 **보상 항이 없습니다**
([`policy.py:29-34`](../act/policy.py#L29-L34)). 시뮬레이터가 주는 `reward`는 데모의 성공
판정과 평가 시 성공률 측정에만 쓰이며, 학습에는 일절 관여하지 않습니다. 순수한 **지도학습
(behavior cloning)**입니다.

## 1.3 이 실험의 목적

| # | 목적 | 확인 방법 |
|---|---|---|
| 1 | **파이프라인 전체를 한 번 끝까지 돌려본다** — 데이터 생성 → 학습 → 평가 | 에러 없이 완주하는가 |
| 2 | **기준 성공률을 확보한다** — 이후 모든 비교 실험의 원점 | 원본 README가 말하는 ≈90%에 도달하는가 |
| 3 | **temporal ensembling의 실제 효과를 측정한다** | 같은 체크포인트로 켜고/끄고 각각 50회 |

3번이 이 보고서의 중심 질문입니다. **모델도 데이터도 완전히 동일하고, 오직 실행 방식만
다른 두 조건을 비교**하므로, 차이가 나온다면 그 원인은 하나로 특정됩니다.

---

# 2. 승 — 실험을 어떻게 설계했는가

## 2.1 시뮬레이션의 기초 조건

MuJoCo 물리엔진 위에 dm_control로 감싼 환경이며, 실물 ALOHA를 본뜬 **ViperX 6-DoF 팔 2개**가
테이블을 사이에 두고 마주 봅니다.

| 항목 | 값 | 비고 |
|---|---|---|
| 태스크 | `sim_transfer_cube_scripted` | 빨간 큐브를 오른팔로 집어 왼팔에 건네기 |
| 제어 주파수 | 50 Hz (`DT = 0.02`) | 실물 ALOHA와 동일 |
| 에피소드 길이 | **400 스텝 = 8초** | 시간이 다 되면 강제 종료 |
| 관측 — 이미지 | `top` 카메라 **1대**, 480×640 RGB | 천장에서 내려다보는 뷰 |
| 관측 — 고유수용 | `qpos` **14차원** | 양팔 6관절 + 그리퍼 1 = 7 × 2 |
| 행동 | **14차원** | 목표 관절각. 위치 제어이지 토크 제어가 아님 |
| 큐브 초기 위치 | **에피소드마다 랜덤** `x∈[0.0, 0.2]`, `y∈[0.4, 0.6]` (m) | [`utils.py:133-142`](../act/utils.py#L133-L142) |
| 큐브 방향 | 고정 (`quat = [1,0,0,0]`) | 회전은 랜덤화되지 않음 |

> **여기가 이 실험에서 가장 중요한 지점입니다.** 큐브 위치가 매번 달라지므로, 정책은 궤적
> 하나를 통째로 외우는 것으로는 풀 수 없습니다. **카메라 영상에서 큐브가 어디 있는지 스스로
> 알아내야만** 합니다.

## 2.2 보상: 성공/실패가 아니라 4단계

성공 여부는 접촉(contact) 판정으로 자동 계산됩니다
([`sim_env.py:143-167`](../act/sim_env.py#L143-L167)).

| 보상 | 조건 | 의미 |
|---|---|---|
| 1 | 오른쪽 그리퍼가 큐브에 닿음 | 접근·정렬 성공 |
| 2 | 닿았고 **테이블에는 안 닿음** | 들어올림 (파지 성공) |
| 3 | 왼쪽 그리퍼가 큐브에 닿음 | 전달 시도 |
| 4 | 왼쪽이 닿았고 테이블에 안 닿음 | **전달 성공** |

- **성공 판정** = 에피소드 400스텝 중 **최고 보상이 4에 도달**했는가.
- **Average return** = 400스텝 동안의 **보상 총합**.

두 지표는 서로 다른 것을 잽니다. return은 보상이 매 스텝 누적되므로 "**얼마나 빨리 단계에
도달해서 얼마나 오래 유지했는가**"를 반영합니다. 즉 **성공률은 되고/안 되고, return은 속도와
안정성**입니다. 이 구분이 뒤에서 결정적인 역할을 합니다.

## 2.3 데이터: 왜 시뮬을 두 번 돌리는가

데모 50개는 사람이 아니라 **스크립트**가 만들었습니다(`_scripted`). 그런데 코드는 에피소드
하나당 시뮬레이터를 **두 번** 돌립니다. 처음 보면 반드시 헷갈리는 부분이라 따로 설명합니다.

```
[1단계] ee_sim_env + scripted_policy
        스크립트가 손끝(end-effector) 위치를 waypoint로 지정해 동작 생성
        → 결과로 나온 관절 궤적 qpos만 뽑아둔다
                    │
                    ▼
[2단계] sim_env
        1단계의 관절 궤적을 action으로 재생하면서
        이미지 / qpos / qvel / action 을 hdf5로 기록
```

**왜 이렇게 하나?** 실물 ALOHA에서 저장되는 것은 손끝 좌표가 아니라 **조종자가 잡고 있는
leader 팔의 관절각**입니다. 시뮬 데이터도 실물과 형식이 1:1로 같아야 하므로, 1단계의 손끝
제어는 데모를 *만드는 수단*일 뿐이고 최종 데이터는 반드시 관절각 형식이어야 합니다.

**IK(역기구학)를 푸는 코드는 레포에 없습니다.** 1단계에서 손끝을 움직이는 방식은 MuJoCo의
mocap body를 로봇 손목에 용접(weld) 구속으로 붙여 두고 그 mocap을 끌면, 구속을 만족시키려고
**물리 솔버가 알아서 관절을 푸는** 구조입니다. 실물에서 사람이 leader 팔을 손으로 끄는 것과
같은 원리입니다.

### 데이터셋 명세

| 항목 | 값 |
|---|---|
| 에피소드 수 | 50 |
| 저장 내용 | `observations/images/top` (400, 480, 640, 3) `uint8`<br>`observations/qpos` (400, 14)<br>`observations/qvel` (400, 14)<br>`action` (400, 14) |
| 용량 | **18 GB** (이미지가 거의 전부) |
| train/val 분할 | 8:2 → **train 40 / val 10 에피소드** ([`utils.py:114-117`](../act/utils.py#L114-L117)) |

> 📌 **hdf5에는 큐브 좌표도, 손끝 좌표도 들어 있지 않습니다.** 이미지·관절각뿐입니다.

### ⭐ 그래서 이 학습은 "따라 하기"가 아니라 "증류"다

여기가 이 실험의 개념적 핵심입니다. **스크립트와 정책이 보는 정보가 다릅니다.**

| | 스크립트 (데모를 만드는 쪽) | ACT 정책 (학습되는 쪽) |
|---|---|---|
| 큐브 위치 | 시뮬 내부 상태에서 **정답 좌표를 그대로 읽음** | ❌ 볼 수 없음 (`env_state = None`) |
| 입력 | 특권 정보 (privileged information) | 카메라 이미지 + 현재 관절각 |

스크립트는 **정답을 알고 푸는 커닝 정책**이고, 정책은 **카메라만 보고 같은 결과를 내야 하는
학생**입니다. 정책이 새로 배워야 하는 것은 ① 픽셀에서 물체 위치 추정 ② 시각→운동 매핑
③ 100스텝 묶음 예측이며, 스크립트에는 이 중 아무것도 없습니다.

**"코드가 만든 데모로 학습하는 게 무슨 의미가 있냐"는 질문의 답이 이것입니다.** 이것은
자기 자신을 베끼는 것이 아니라, 특권 정보를 쓰는 교사에서 관측만 쓰는 학생으로의
**증류(distillation)**입니다.

## 2.4 모델 구조

| 구성 | 값 | 출처 |
|---|---|---|
| 이미지 백본 | **ResNet18** (ImageNet 사전학습) | 카메라 1대 → feature map |
| Transformer 인코더 | **4층**, 8 heads | |
| Transformer 디코더 | **7층**, 8 heads | 100개 query → 100개 행동 |
| hidden dim | 512 | |
| feedforward dim | 3200 | |
| CVAE 인코더 | 4층 Transformer, 잠재변수 `z` **32차원** | 학습 시에만 사용 |
| chunk size `k` | **100** (= 2초) | |
| **총 파라미터** | **83.92 M** | `train.log` 실측 |

## 2.5 학습 설정 및 결과

```bash
python3 imitate_episodes.py \
  --task_name sim_transfer_cube_scripted \
  --policy_class ACT --kl_weight 10 --chunk_size 100 --hidden_dim 512 \
  --batch_size 8 --dim_feedforward 3200 --num_epochs 2000 --lr 1e-5 --seed 0
```

- **손실** = `L1(정답 행동열, 예측 행동열) + 10 × KL`
- optimizer AdamW, lr `1e-5` (백본도 `1e-5`), weight decay `1e-4`
- 모든 하이퍼파라미터는 **원본 README의 권장값 그대로**입니다. 튜닝하지 않았습니다.
  (목적 1·2가 "원본 재현"이므로 변수를 만들지 않으려는 의도적 선택)

| 항목 | 값 |
|---|---|
| GPU | RTX 2080 Ti (11 GB) |
| 소요 시간 | **38분 10초** (2000 epoch, epoch당 1.15초) |
| 최대 VRAM | 4,840 MiB / 11,264 MiB |
| **best val loss** | **0.045839 @ epoch 1995** |
| 산출물 | 체크포인트 23개 (7.2 GB) |

> ⚠️ **best가 마지막 근처(1995/2000)에서 나왔다는 것은 val loss가 아직 떨어지는 중, 즉
> 수렴하지 않았다는 뜻입니다.** 이 사실이 뒤에서 흥미로운 결과로 이어집니다([3.6](#36-val-loss는-수렴하지-않았다-그런데도-98가-나왔다)).

## 2.6 평가 프로토콜

`policy_best.ckpt` **하나**를 가지고, 다른 조건 없이 실행 방식만 바꿔 각각 50회 rollout:

| | 조건 A | 조건 B |
|---|---|---|
| 플래그 | (없음) | `--temporal_agg` |
| 추론 횟수 | 100스텝마다 1회 = **에피소드당 4회** | 매 스텝 = **에피소드당 400회** |
| 행동 선택 | 예측된 chunk를 그대로 재생 | 과거 예측들을 지수가중 평균 |

**두 실행은 완전히 짝지어져 있습니다(paired).** 평가 루프 진입 전에 `set_seed(1000)`을 한 번
걸고([`imitate_episodes.py:152`](../act/imitate_episodes.py#L152)), 루프 안에서 전역 난수를
쓰는 곳은 큐브 위치 샘플링뿐입니다. 따라서 **n번째 rollout의 큐브 위치는 두 실행에서 동일**
합니다. 이 덕분에 "같은 장면에서 A는 실패, B는 성공"이라는 직접 비교가 가능합니다.

---

# 3. 전 — 무엇이 나왔는가

## 3.1 주 결과

| | temporal_agg **끔** | temporal_agg **켬** |
|---|---|---|
| **Success rate** | **0.94** (47/50) | **0.98** (49/50) |
| Average return | 592.66 | **669.04** |
| 소요 시간 | 9분 32초 | 15분 19초 (**1.6배**) |

원본 README가 제시한 기대치 "약 90%"를 **양쪽 조건 모두 넘겼습니다.** 목적 1·2 달성입니다.

**성공률은 +4%p인데 average return은 +12.9%입니다.** 이는 모순이 아니라 두 지표가 다른 것을
재기 때문입니다([2.2](#22-보상-성공실패가-아니라-4단계) 참조). temporal ensembling은 실패
2건을 살렸을 뿐 아니라, **원래 성공하던 것들도 더 빨리·더 안정적으로** 만들었습니다.

> 💡 추론을 100배 더 하는데(4회 → 400회) 실행 시간은 1.6배밖에 안 늘었습니다. 병목이 신경망이
> 아니라 **물리 시뮬레이션과 렌더링**에 있다는 뜻입니다.

## 3.2 단계별 분해 — 어디서 실패하는가

| 도달 보상 | 의미 | agg 끔 | agg 켬 |
|---|---|---|---|
| ≥ 1 | 오른팔이 큐브에 접촉 | 48/50 (96%) | **50/50 (100%)** |
| ≥ 2 | 큐브를 들어올림 | 48/50 (96%) | 49/50 (98%) |
| ≥ 3 | 왼팔로 전달 시도 | 47/50 (94%) | 49/50 (98%) |
| ≥ 4 | **전달 성공** | 47/50 (94%) | 49/50 (98%) |

세로로 읽으면 명확합니다. **≥2에서 ≥4까지 거의 떨어지지 않습니다.**
일단 큐브를 손에 쥐면 양팔 전달은 사실상 결정적으로 따라옵니다.

이유는 구조적입니다. 전달 동작은 큐브의 초기 위치와 무관하게 **거의 고정된 궤적**이라 정책이
쉽게 외웁니다. 반면 접근·파지는 **매번 달라지는 큐브 위치를 픽셀에서 추정해야 하는 유일한
지점**이라, 오차가 전부 여기로 몰립니다.

## 3.3 실패 4건 전수 분석

50회 × 2조건 = 100회 중 실패는 4건뿐이므로 전수 조사가 가능했습니다.
(`Rollout N` ↔ `videoN.mp4` 1:1 대응)

| rollout | 조건 | 최고 보상 | return | 무슨 일이 있었나 |
|---|---|---|---|---|
| **R6** | agg 끔 | **0** | 0 | 큐브에 **닿지도 못함** |
| **R7** | agg 끔 | **0** | 0 | 큐브에 **닿지도 못함** |
| **R9** | agg 끔 | 2 | 4 | 집었다가 **놓침** |
| **R9** | agg 켬 | 1 | 4 | 닿기만 하고 **못 집음** |

여기서 두 가지가 드러납니다.

**(1) 주된 실패는 파지가 아니라 접근이다.** R6·R7은 보상 0, 즉 큐브에 접촉조차 못 했습니다.
"실패는 파지 단계에 몰려 있다"는 것이 표만 보면 자연스러운 해석이지만, 실제로는 그보다 한
단계 앞인 **접근·정렬**이 진짜 병목입니다.

**(2) R9는 두 조건 모두에서 실패했습니다.** 두 실행이 짝지어져 있으므로([2.6](#26-평가-프로토콜))
이는 조건의 문제가 아니라 **그 큐브 위치 자체가 이 정책에게 어렵다**는 뜻입니다.
게다가 agg를 켜면 최고 보상이 **2 → 1로 오히려 낮아졌습니다.** 평균적으로는 개선이지만
**에피소드 단위로는 항상 우월하지 않다**는 반례입니다.

## 3.4 같은 장면에서의 직접 비교 — R6, R7

R6·R7은 agg를 켜자 **실패에서 성공(보상 4)으로 바뀌었습니다.** 큐브 위치가 동일하므로,
**동일 장면의 실패 영상과 성공 영상을 나란히 놓고 볼 수 있는 유일한 케이스**입니다.
temporal ensembling이 궤적에 무엇을 하는지 눈으로 확인할 수 있는 지점입니다.

짝비교 결과를 정리하면:

```
agg가 살린 것 : 2건 (R6, R7)
agg가 망친 것 : 0건
양쪽 다 실패  : 1건 (R9)
```

## 3.5 숨어 있던 신호 — "간신히 성공한" 케이스

단계별 표에는 드러나지 않지만, rollout별 return 분포를 보면 더 뚜렷한 차이가 있습니다.

```
agg 끔  성공 47건 중 return 최저:  R34 = 211,  R5 = 213,  그다음은 588
agg 켬  성공 49건 중 return 최저:  R26 = 610
```

agg를 끄면 **211·213짜리가 2건** 있습니다. 성공 판정은 받았지만 400스텝 중 거의 끝에 가서야
전달을 마쳤다는 뜻입니다. 즉 **한참 헤매다 겨우 성공한 케이스**입니다. agg를 켜면 그런 사례가
**하나도 없습니다**(최저 610).

> **성공률 +4%p보다 이 사실이 temporal ensembling의 효과를 더 잘 보여줍니다.**
> 성공률은 이분법이라 "겨우 성공"과 "깔끔한 성공"을 구별하지 못합니다.
> 실물 로봇이라면 이 차이가 곧 신뢰성의 차이입니다.

흥미롭게도 R34·R5는 큐브가 `x ≈ 0.014`로 **범위의 정반대 끝**에 있었습니다.

## 3.6 큐브 위치와 실패의 관계 (탐색적 분석)

평가 시드가 고정이므로 50개 rollout의 큐브 위치를 사후에 재현할 수 있었습니다.

| 큐브 x 사분위 | n | agg 끔 | agg 켬 | 평균 return (끔) |
|---|---|---|---|---|
| x ∈ [0.000, 0.047] | 13 | 13/13 | 13/13 | 596.4 |
| x ∈ [0.047, 0.106] | 12 | 12/12 | 12/12 | 645.5 |
| x ∈ [0.106, 0.159] | 12 | 11/12 | 11/12 | 598.2 |
| **x ∈ [0.159, 0.197]** | 13 | **11/13** | 13/13 | **535.0** |

실패한 R6(x=0.186, 상위 7%)·R7(x=0.196, 상위 2%)은 모두 **x 범위의 먼 쪽 끝**에 있었습니다.
평균 return도 이 구간에서 가장 낮습니다.

**그러나 이 설명은 완결되지 않습니다.** x가 가장 큰 R45(x=0.1968)는 두 조건 모두 성공했고,
x와 return의 상관계수도 **-0.162**로 약합니다. 즉 **위치가 실패를 설명하는 유일한 변수는
아닙니다.** 각 구간 n=13 수준이라 통계적 검정력도 부족합니다.
→ 이 항목은 **가설이지 결론이 아닙니다.**

## 3.7 val loss는 수렴하지 않았다. 그런데도 98%가 나왔다

best val loss가 epoch 1995(마지막 직전)에서 나왔다는 것은 더 학습하면 loss가 더 내려간다는
뜻입니다. 그런데도 성공률은 94~98%였습니다.

이는 **L1 손실과 태스크 성공률이 같은 것을 재지 않는다**는 것을 보여줍니다. 손실은 400스텝
전체의 오차를 균등하게 평균 내지만, 성공과 실패는 **파지 직전 몇 스텝**에서 갈립니다. 궤적의
대부분(이동·전달 구간)에 남아 있는 오차는 loss를 올리지만 성공률은 건드리지 않습니다.

> 이 발견으로 **당초 계획했던 `--num_epochs 5000` 재학습을 취소했습니다.**
> ("loss를 더 낮춰야 성공률이 오른다"는 직관이 항상 맞지는 않습니다.)
>
> 단, 원본 README는 *"성공률과 부드러움은 loss가 평탄해진 뒤에도 한참 더 좋아질 수 있으니
> 더 오래 학습하라"*고 권합니다. 즉 **더 돌렸으면 더 좋아졌을 가능성도 열려 있습니다.**
> 이 실험이 보인 것은 "더 돌릴 필요가 없다"가 아니라 **"이 정도로도 목표치는 나온다"**입니다.

---

# 4. 결 — 무엇을 알게 되었는가

## 4.1 결론

1. **파이프라인 전체가 검증되었습니다.** 데이터 생성 → 학습 → 평가가 에러 없이 완주했고,
   기준 성공률 **94% / 98%**를 확보했습니다. 이후 모든 비교 실험의 원점이 생겼습니다.
2. **병목은 접근·파지 단계 하나로 특정되었습니다.** 일단 집으면 전달은 따라옵니다.
   개선하고 싶다면 손을 대야 할 곳은 여기입니다.
3. **temporal ensembling은 켤 가치가 있습니다.** 성공률 +4%p, average return +12.9%,
   그리고 "간신히 성공"하는 불안정한 사례를 없앴습니다. 비용은 실행 시간 1.6배입니다.
4. **temporal ensembling이 고칠 수 있는 실패는 이미 다 고쳤습니다.** 개선이 전부 `≥1`(접촉)
   단계에서 나왔다는 것은 이것이 **궤적을 매끄럽게 만드는** 장치임을 뜻합니다. 남은 R9는
   위치 추정 자체가 틀린 경우로 보이며, 평활화로는 고쳐지지 않습니다.
5. **val loss는 성공률의 대리 지표로 신뢰할 수 없습니다.**

## 4.2 논문과의 대조

| | 논문 (Zhao et al., 2023, Table I) | 이 실험 |
|---|---|---|
| Cube Transfer, scripted | **97%** | **94%** (agg 끔) / **98%** (agg 켬) |
| 평가 규모 | 3 seed × 50회 = **150회** | 1 seed × 50회 = **50회** |
| temporal ensembling | **논문에 명시 없음** | 두 조건 모두 측정 |

**재현에 성공했다고 볼 수 있습니다.** 논문의 97%는 이 실험의 두 조건 사이에 놓입니다.

논문의 다른 주장 두 가지도 이 실험과 부합합니다.

- **temporal ensembling 이득**: 논문 ablation은 ACT에서 **+3.3%**를 보고했고,
  이 실험은 +4%p였습니다.
- **scripted 데이터에서는 CVAE가 거의 무의미**: 논문은 *"scripted 데이터로 학습할 때 CVAE
  목적함수를 제거해도 성능 차이가 거의 없다. 반면 human 데이터에서는 35.3% → 2%로 급락한다"*
  고 보고합니다. 이 실험의 학습 로그에서도 KL 항이 0.002 수준까지 떨어졌는데, 이는
  **`z`가 사실상 아무 정보도 나르지 않는 상태**로 볼 수 있습니다.

## 4.3 한계

이 실험의 결과를 읽을 때 반드시 함께 고려해야 할 것들입니다.

**(1) 데이터에 멀티모달리티가 없습니다.** 데모 생성 코드에 `inject_noise = False`가 하드코딩
되어 있어([`record_sim_episodes.py:31`](../act/record_sim_episodes.py#L31)), 큐브 위치가 같으면
궤적이 **항상 완전히 동일**합니다. 노이즈 주입 코드는 존재하지만 쓰이지 않습니다.
→ 따라서 **94/98%는 알고리즘이 가장 유리한 조건에서 낸 상한값**이며, ACT의 핵심 장치인
CVAE는 실질적으로 검증되지 않았습니다.

**(2) 표본이 50회입니다.** 짝비교에서 불일치 쌍이 2건(2:0)뿐이므로, **+4%p라는 차이는
통계적으로 유의하다고 말하기 어렵습니다**(McNemar 검정 관점에서 n이 너무 작음).
방향성은 average return(+12.9%)과 "간신히 성공" 사례 소멸이 함께 뒷받침하지만,
**성공률 차이 하나만으로 결론을 내려서는 안 됩니다.**

**(3) 시드 1개, 카메라 1대, 태스크 1개입니다.** 학습 시드도 `0` 하나이므로 학습 자체의
분산은 측정되지 않았습니다.

**(4) 전부 시뮬레이션입니다.** 실물의 조명 변화, 마찰 불확실성, 센서 지연, 캘리브레이션
오차는 일절 없습니다. **이 성공률이 실물로 이전된다고 볼 근거는 이 실험에 없습니다.**

## 4.4 다음에 할 것

| 우선순위 | 실험 | 무엇을 알 수 있나 |
|---|---|---|
| **1** | `sim_transfer_cube_human` 데이터로 동일 학습 | 사람 데모의 멀티모달리티가 왜 어려운지, **CVAE가 왜 필요한지**. 한계 (1)의 정면 돌파 |
| 2 | `inject_noise=True`로 데이터 재생성 후 비교 | 데이터 다양성과 복원력의 트레이드오프 |
| 3 | 평가 시드를 바꿔 3회 반복 | 한계 (2) 해소 — +4%p가 진짜인지 판별 |
| 4 | `chunk_size` 50 / 200 비교 | 논문 ablation의 직접 재현 |
| 5 | act-plus-plus로 Diffusion Policy 학습 | 다른 알고리즘과의 비교 |

> 1번을 권합니다. 지금 **기준값(94/98%)이 확보된 상태**이므로, `_human`을 붙이면
> "코드가 만든 데모 vs 사람이 만든 데모"라는 이 보고서의 핵심 주장([2.3](#-그래서-이-학습은-따라-하기가-아니라-증류다))을
> **숫자로 직접 확인**할 수 있습니다. 코드 수정도 필요 없습니다.

---

# 5. 확인하지 못한 것 — 해석은 읽는 사람의 몫

이 보고서를 쓰면서 **확인하려 했으나 확인하지 못한 것**들입니다. 추측으로 메우지 않고
그대로 남깁니다.

### (1) 논문 Table I의 시뮬 수치가 temporal ensembling을 켠 것인지 **모릅니다**
논문 본문에 명시가 없습니다. ablation에서는 *"chunking의 효과만 측정하기 위해 temporal
ensembling을 끈다"*고 따로 밝히므로 Table I은 켰을 가능성이 있지만, **확정할 수 없습니다.**
→ 이 때문에 "97% vs 94%/98%" 비교를 얼마나 엄밀하게 받아들일지는 판단이 필요합니다.

### (2) 논문이 시뮬에서 몇 epoch 학습했는지 **모릅니다**
논문은 *"RTX 2080 Ti 1대로 5시간"*이라고만 적고 있습니다. 이 실험은 같은 GPU로 **38분**
(2000 epoch)이 걸렸습니다. 5시간이 시뮬 기준인지 실물 기준인지, epoch 수가 몇인지 확인하지
못했습니다. → **논문보다 훨씬 적게 학습하고도 비슷한 성공률이 나온 것인지**, 아니면 조건이
다른 것인지는 미결입니다.

### (3) R9가 **왜** 실패하는지 모릅니다
큐브 위치(x=0.132, y=0.553)는 범위의 극단이 아닙니다. 두 조건 모두에서 실패했고, agg를 켜면
오히려 더 이른 단계에서 멈췄습니다. → **영상을 직접 보고 판단해야 하는 부분입니다.**
`failures/` 폴더에 따로 모아 두었습니다.

### (4) x가 가장 큰 R45는 성공하는데 R6·R7은 실패한 이유를 모릅니다
[3.6](#36-큐브-위치와-실패의-관계-탐색적-분석)의 "먼 쪽 끝이 어렵다" 가설로는 설명되지
않습니다. y좌표, 접근 각도, 혹은 위치와 무관한 다른 요인일 수 있습니다.
→ **영상 비교로만 좁힐 수 있습니다.**

### (5) 이 정책이 학습 데이터 분포 밖에서 어떻게 되는지 모릅니다
큐브를 `x∈[0,0.2], y∈[0.4,0.6]` **바깥**에 두는 실험은 하지 않았습니다.
→ 정책이 위치를 진짜로 추정하는지, 아니면 좁은 범위에 과적합된 것인지는 **미검증**입니다.

### (6) 학습 자체의 분산을 모릅니다
시드 0 하나로만 학습했습니다. 다른 시드로 학습하면 94%가 아니라 88%나 99%가 나올 수도
있습니다. → 이 실험의 수치는 **점 추정치이며 신뢰구간이 없습니다.**

---

# 부록 A — 재현 명령

```bash
conda activate act

# ⚠️ 비대화형 셸에서는 ~/.bashrc가 적용되지 않는다. 렌더링 변수를 명시적으로 export할 것.
#    (생략하면 소프트웨어 렌더링으로 떨어져 약 4.7배 느려진다)
export ALOHA_DATA_DIR=~/aloha_project/aloha_data \
       MUJOCO_GL=glfw PYOPENGL_PLATFORM=glx GALLIUM_DRIVER=d3d12
cd ~/aloha_project/act

# 1) 데모 50개 생성 (약 18 GB)
python3 record_sim_episodes.py \
  --task_name sim_transfer_cube_scripted \
  --dataset_dir $ALOHA_DATA_DIR/sim_transfer_cube_scripted \
  --num_episodes 50

# 2) 학습 (RTX 2080 Ti에서 약 38분)
python3 imitate_episodes.py \
  --task_name sim_transfer_cube_scripted \
  --ckpt_dir ~/aloha_project/ckpt/sim_transfer_cube_scripted_act \
  --policy_class ACT --kl_weight 10 --chunk_size 100 --hidden_dim 512 \
  --batch_size 8 --dim_feedforward 3200 --num_epochs 2000 --lr 1e-5 --seed 0

# 3) 평가 — 위 명령에 --eval 추가 (policy_best.ckpt를 불러온다)
#    temporal ensembling을 쓰려면 --temporal_agg 추가
```

> ⚠️ **평가를 두 번 돌리면 rollout 영상이 덮어써집니다.** `video0.mp4` ~ `video49.mp4`라는
> 고정 이름으로 `ckpt_dir`에 저장되기 때문입니다. 1회차 결과를 먼저 다른 폴더로 옮기십시오.

> ⚠️ **저장되는 영상은 코덱이 `mp4v`(MPEG-4 Part 2)라 브라우저·VS Code에서 재생되지
> 않습니다.** `ffmpeg -i in.mp4 -c:v libx264 -pix_fmt yuv420p out.mp4`로 H.264 변환이 필요합니다.

# 부록 B — 산출물 위치

```
~/aloha_project/
├── aloha_data/sim_transfer_cube_scripted/   데모 50개 (18 GB)
└── ckpt/
    ├── train.log                            학습 로그
    ├── eval_no_agg.log                      평가 1회차 (rollout별 결과 포함)
    ├── eval_temporal_agg.log                평가 2회차
    └── sim_transfer_cube_scripted_act/
        ├── policy_best.ckpt                 평가에 쓰인 체크포인트
        ├── train_val_*_seed_0.png           학습 곡선
        ├── rollouts_no_agg/                 원본 영상 50개 (mp4v, 재생 불가)
        ├── rollouts_temporal_agg/           원본 영상 50개 (mp4v, 재생 불가)
        ├── rollouts_no_agg_h264/            H.264 변환본 (재생 가능)
        ├── rollouts_temporal_agg_h264/      H.264 변환본 (재생 가능)
        └── failures/                        ⭐ 실패 케이스 모음 (아래)
```

`failures/` — 본문에서 언급한 케이스를 이름 붙여 모아 둔 것:

| 파일 | 내용 |
|---|---|
| `noagg_R6_FAIL_reward0_no-contact.mp4` | 접촉 실패 |
| `noagg_R7_FAIL_reward0_no-contact.mp4` | 접촉 실패 |
| `noagg_R9_FAIL_reward2_grasp-then-drop.mp4` | 집었다 놓침 |
| `agg_R9_FAIL_reward1_touch-only.mp4` | 닿기만 함 (양쪽 다 실패한 유일한 케이스) |
| `agg_R6_SUCCESS_compare-with-noagg_R6.mp4` | ⭐ R6의 성공본 — 실패본과 **같은 큐브 위치** |
| `agg_R7_SUCCESS_compare-with-noagg_R7.mp4` | ⭐ R7의 성공본 — 실패본과 **같은 큐브 위치** |
| `noagg_R34_ok-but-barely_return211.mp4` | 간신히 성공 (return 211) |
| `noagg_R5_ok-but-barely_return213.mp4` | 간신히 성공 (return 213) |

# 참고 문헌

1. Tony Z. Zhao, Vikash Kumar, Sergey Levine, Chelsea Finn.
   **"Learning Fine-Grained Bimanual Manipulation with Low-Cost Hardware."**
   arXiv:2304.13705 (2023). — ACT 알고리즘과 ALOHA 하드웨어의 원 논문.
   https://arxiv.org/abs/2304.13705 · 프로젝트: https://tonyzhaozh.github.io/aloha/
2. Zipeng Fu, Tony Z. Zhao, Chelsea Finn.
   **"Mobile ALOHA: Learning Bimanual Mobile Manipulation with Low-Cost Whole-Body
   Teleoperation."** arXiv:2401.02117 (2024), CoRL 2024. — ALOHA에 이동 베이스를 붙인 후속
   연구. 이 실험에서 직접 쓰지는 않았으나, 태스크당 50개 데모라는 규모 감각과 co-training의
   효과를 참고.
   https://arxiv.org/abs/2401.02117 · 프로젝트: https://mobile-aloha.github.io/
3. 구현 코드: https://github.com/tonyzhaozh/act

# 관련 문서

- [docs/00-environment.md](docs/00-environment.md) — 환경 구축 상세
- [docs/01-act-sim.md](docs/01-act-sim.md) — ACT 시뮬 개념 정리
- [docs/90-troubleshooting.md](docs/90-troubleshooting.md) — 렌더링 등 문제 해결
- [logs/2026-09-18.md](logs/2026-09-18.md) — 이 실험의 당일 작업 기록
- [NEXT.md](NEXT.md) — 다음 작업
